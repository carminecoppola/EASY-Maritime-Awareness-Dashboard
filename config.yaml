app:
  host: 0.0.0.0
  port: 5000
  debug: false

rgb:
  enabled: true
  mode: split_stereo
  camera_index: 0
  width: 1280
  height: 480
  fps: 10
  quality: 85
  crop_ratio: 0.5

thermal:
  enabled: true
  mode: real
  threshold_celsius: 35.0
  delta_threshold: 8.0
  device: /dev/video0

events:
  max_events: 200
