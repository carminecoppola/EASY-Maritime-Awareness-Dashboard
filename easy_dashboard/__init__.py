"""Internal application package for the EASY dashboard.

The goal of this package is to keep the Flask entrypoint small and make the
project easier to navigate: config, storage, media helpers, hardware adapters,
and view payload builders each live in their own module.
"""

